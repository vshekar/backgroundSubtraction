# -*- mode: python -*-
a = Analysis(['bkgsub.py'],
             pathex=['C:\\Users\\vesheka\\Desktop\\Python code\\Final SMP Code'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='bkgsub.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='bkgsub')
